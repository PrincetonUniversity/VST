#!/bin/bash
BENCHMARK=$1
SOLVER=$2
NUMBERV=${SOLVER##*/list_solver}
NUMBER=${NUMBERV%.v}
OUTPUT=${BENCHMARK%.v}$NUMBER.v
# echo $OUTPUT
echo "Require Import VST.floyd.proofauto." > $OUTPUT
echo "Require Import VST.floyd.list_solver"$NUMBER"." >> $OUTPUT
tail +3 $1 >> $OUTPUT
